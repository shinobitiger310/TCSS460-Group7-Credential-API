# TCSS-460-auth-squared Template - Environment Configuration
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=8000
NODE_ENV=development

# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=auth-squared
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_password

# For local development without SSL
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/auth-squared?sslmode=disable

# JWT Configuration
# IMPORTANT: Generate a secure random string for production
# Example: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars
JWT_EXPIRY=14d

# Email Configuration (for email verification and password reset)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=Auth² Service <noreply@auth2.com>
SEND_EMAILS=false

# Application Configuration
APP_BASE_URL=http://localhost:8000

# Optional: Twilio SMS Configuration (for phone verification)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=your-twilio-phone-number
